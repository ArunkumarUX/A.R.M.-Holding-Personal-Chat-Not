/** Re-export — canonical prompt lives in apparelGroupPptPrompt.mjs */
export {
  PRESENTATION_BUILDER_SYSTEM,
  buildApparelGroupPptPrompt,
  buildPerceptisImplementationPrompt,
  buildPerceptisPromptFromPayload,
  buildCompactPerceptisPrompt,
  serializeSlidesForPerceptis,
} from './apparelGroupPptPrompt.mjs';

export { SLIDE_TYPE_LABELS } from './presentationBuilderPrompts.labels.mjs';
